class Endpoint {
    static readonly BASE_URL = 'http://localhost:3000';
    static readonly LOGIN = '/signin/login/';
    static readonly LOGOUT = '/signin/logout/';

    // Add more endpoints as needed
}

export default Endpoint;